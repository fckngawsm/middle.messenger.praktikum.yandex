{{#> AuthContainer}}
    {{#> FormWrapper}}
      <h1 class="form__title">Вход</h1>
      {{#> Form id="login-form"}}
          {{> Input id="login" type="text" placeholder="Логин" name='login' label="Логин" required="true" helperText="Неверный логин"}}
          {{> Input id="password" type="password" placeholder="Пароль" name="password" label="Пароль" required="true"}}
      {{/Form}}
      {{> Spacer}}
      <div class="button__group">
          <a href="{{buttonLink.href}}" class="{{button.class}}" id="{{button.id}}">
            {{button.text}}
          </a>
          {{> Link class=link.class id=link.id href=link.href linkText=link.linkText}}
      </div>
    {{/FormWrapper}}
{{/AuthContainer}}